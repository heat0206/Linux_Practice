# Glow - the Terminal md file renderer

This bash command helps render an md file inside the terminal.

**Command**

```bash
glow [filename]
```

**Installation Source**

installed using the command `sudo snap install glow`
